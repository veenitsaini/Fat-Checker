# Copy this file to .streamlit/secrets.toml for LOCAL development only.
# Never commit your real secrets.toml to GitHub — it's already in .gitignore.
#
# On Streamlit Community Cloud, you do NOT use this file — instead paste the
# same content into your app's "Settings -> Secrets" box in the dashboard.

ANTHROPIC_API_KEY = "sk-ant-your-key-here"
